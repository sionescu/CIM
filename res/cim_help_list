Usage:

	cim list <subcommand>

Description:
List available target for subcommand.
If no subcommands are given, list available target for 'list'

Examples:

	# list available lisp implementation to install
	cim list install

	# equivalent to 'cim list list'
	cim list

	# targets for subsubcommands are also available
	cim list resume rm